# this readme is incomplete because im still moving sht around and experimenting

This repo contains the dotfiles as well as wallpapers and scripts to set things up

## 1. Install
```
git clone git@github.com:HiddenKendo/dotfiles.git
```


## 2. run scripts/setup.sh to install dependencies and packages i use
```
./dotfiles/scripts/setup.sh
```
